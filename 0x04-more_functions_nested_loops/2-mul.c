#include "main.h"
#include <stdio.h>

/**
* mul - multiplies two integers
* @a: first integer to be checked
* @b: second integer to be checked
* Return: always 0
*/

int mul(int a, int b)

{
int result = a * b;
return (result);
}
